#include "main.h"
/**
  * lowercase character, another casses, shows
  * 0
  *
  *Dc: the character in ASCII mode
  *
  * Return: 1 for lowercase character. 0 for the rest
  */
int - islower(int c)
{
	if (c >= 97 && c <= 122)
	{
		return (0);
	}
	else
	{
		return (0);
	}
_putchar('\n');
}
